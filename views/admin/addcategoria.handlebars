{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h2>Cadastro de Novas categorias</h2>
<div class="card">
    <div class="card-body">
        <form action="/admin/categorias/nova" method="post">
            <label class="h5" for="nome"> Nome </label>
                <input class="form-control" type="text" name="nome" id="nome" placeholder="Nome da categoria">

            <label class="h5 mt-4" for="slug"> Slug </label> <!-- Slug é a URLm ou link para a tal categoria.-->
                <input class="form-control" type="text" name="slug" id="slug" placeholder="Slug da Categoria">
            <button  type="submit" class="btn btn-success text-dark mt-3">Criar Catrgoria</button>
        </form>
    </div>

</div>