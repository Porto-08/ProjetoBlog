{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h2>Editar Categorias</h2>
<div class="card">
    <div class="card-body">
        <form action="/admin/categorias/edit" method="post">
            <input type="hidden" name="id" id="{{categorias._id}}" value="{{categorias._id}}"> <!-- Input invisivel para recuperar o id da categoria. -->
            <label class="h5" for="nome"> Nome </label>
                <input class="form-control" type="text" name="nome" id="nome" placeholder="Nome da categoria" value="{{categorias.nome}}">

            <label class="h5 mt-4" for="slug"> Slug </label> <!-- Slug é a URLm ou link para a tal categoria.-->
                <input class="form-control" type="text" name="slug" id="slug" placeholder="Slug da Categoria" value="{{categorias.slug}}">
            <button  type="submit" class="btn btn-success text-dark mt-3">Finalizar Edição</button>
        </form>
    </div>
</div>

